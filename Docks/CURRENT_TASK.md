# CURRENT_TASK.md — تمرکز فعلی سیستم

## ۱. درخت تمرکز (Focus Tree)
```
src/
├── pages/
│   └── PageDetail.tsx                      [در حال فعال — تسک F2.6]
└── components/
    └── TemporalBadge.tsx                   [در حال فعال — تسک F2.6]
```

## ۲. جزئیات تکمیلی تسک فعلی (تسک F2.6)

### عنوان و هدف
**تسک F2.6 — UI: Quick Toggle + اعمال Boost در PageDetail.tsx**
افزودن سوییچ سریع در هدر `PageDetail` جهت فعال/غیرفعال‌سازی هوشمندسازی فصلی برای صفحه جاری، اعمال `applyTemporalBoost` روی کاندیداهای نمایش داده شده، اضافه شدن `TemporalBadge` به صورت absolute روی کارت‌های کاندیدایِ بهبود یافته، و انتقال مناسبت‌ها به `analysisService.runSinglePageAnalysis` برای ادغام ترتیب جدید در پردازش با هوش مصنوعی.

### راهنمای پیاده‌سازی فنی

۱. **ایجاد کامپوننت جدید `src/components/TemporalBadge.tsx`:**
   - نمایش نمادین و چشم‌نواز میزان افزایش امتیاز (مثلاً `+4x` برای پیش‌واز به رنگ سبز، `+3x` برای جاری به رنگ آبی، یا جریمه برای موارد خارج از فصل) مبتنی بر `temporalLabel` و `temporalMultiplier`.

۲. **ویرایش `src/pages/PageDetail.tsx`:**
   - تزریق `useTemporalContext` و استفاده از `applyTemporalBoost` و `sortByBoostedScore` به همراه `useMemo` بر روی لیست کاندیداها.
   - ردیف دکمه‌های هدر: افزودن Quick Toggle شبیه به Badgeها که وضعیت فعال بودن هوشمندسازی زمانی برای این صفحه جزییات را تغییر می‌دهد.
   - قرار دادن `TemporalBadge` به صورت هوشمند بر روی گزینه‌های مشمول ضریب.
   - پاس دادن مناسبت‌ها به سیستم هوش مصنوعی در زمان اجرای درخواست تحلیل تک‌صفحه‌ای.

## ۳. وضعیت تسک‌های بعدی

- **تسک F2.5 — UI: بخش تنظیمات Live در Config.tsx** [کامل شده ✅]
- **تسک F2.6 — UI: Quick Toggle + اعمال Boost در PageDetail.tsx** [کامل شده ✅]

---

## ۴. رله کانتکست (Context Relay)

### وضعیت پروژه و کارهای قبلی:
- طراحی، پیاده‌سازی و یکپارچه‌سازی لایه گرافیکی «هوشمندسازی فصلی-زمانی (Live Boost)» در صفحه `Config.tsx` شامل سوییچ سراسری، دانلود تمپلیت، آپلود و اعتبارسنجی فایل CSV مناسبت‌ها با Zod و نمایش جدول تعاملی پیش‌نمایش رویدادهای سفارشی به همراه قابلیت حذف انتخابی/کلی با موفقیت کامل به پایان رسید.
- کمپایل اپلت بدون هیچ نقصی موفقیت‌آمیز بود.
- در تسک پیش‌رو (F2.6) باید سوئیچ سریع محلی، ردیابی ضرایب زمانی و تغییر اولویت‌بندی در میز کار صفحه جزییات به همراه هماهنگی تحلیل هوش مصنوعی پیاده‌سازی و نهایی شود.
