# CURRENT_TASK.md — تمرکز فعلی سیستم

## ۱. درخت تمرکز (Focus Tree)
```
src/
├── services/temporal/
│   └── temporalService.ts                  [کانون اصلاح — تسک F2.7]
├── services/analysis/
│   └── analysisService.ts                  [هم‌ترازی مسیر AI — تسک F2.7]
├── pages/
│   └── PageDetail.tsx                       [تخلیه منطق از UI — تسک F2.7]
└── components/
    └── TemporalBadge.tsx                    [نمایش زامبی/ضرایب صحیح — تسک F2.7]
```

## ۲. جزئیات تکمیلی تسک فعلی (تسک F2.7 — دیباگ و یکپارچه‌سازی)

### عنوان و هدف
**تسک F2.7 — دیباگ و یکپارچه‌سازی عینک لایو (Re-architecture)**
بازگرداندن کل منطق «عینک لایو» به یک پایپ‌لاین Pure تک‌مرجع در `temporalService.ts` و صدا زدن آن از هر دو مسیر (نمایش و Gemini) به‌طور یکسان. رفع باگ‌های «جدول ۸‌تایی»، «نمایش ماه‌های اشتباه»، و «واگرایی ترتیب UI/AI». الگوریتم پایه (`scorer.ts`/`idfCalculator.ts`/Dexie) دست‌نخورده می‌ماند.

> شرح کامل: شش قانون قطعی در `PROJECT.md` و `ARCHITECTURE.md §۱۰`؛ جدول باگ‌ها (A–H) و راهنمای فنی دقیق در `tasks.md → تسک F2.7`.

### خلاصه اصلاحات
- **`temporalService.ts`:** ضرایب صحیح (pre=×4، current=×3)، پنجره مجاز بدون شرط + استثنای رویداد جاری + فصل بعد ژنریک در آخر فصل، تابع orchestrator جدید `buildLiveOrderedList` با سورت اکید، `PIN_QUOTA = 4`، صفر `slice`.
- **`PageDetail.tsx`:** حذف کامل `slice(0,5)/slice(0,3)`/فیلتر/سورت محلی؛ صرفاً `buildLiveOrderedList` صدا زده شود؛ پین ۴ ردیف اول صرفاً بصری.
- **`analysisService.ts`:** استفاده از همان `buildLiveOrderedList` قبل از `top30` تا ترتیب Az با نمایش یکسان شود.
- **`TemporalBadge.tsx`:** افزودن حالت زامبی و هم‌خوانی برچسب‌ها با ضرایب.

## ۳. وضعیت تسک‌ها

- **تسک F2.5 — UI: بخش تنظیمات Live در Config.tsx** [کامل شده ✅]
- **تسک F2.6 — UI: Quick Toggle + اعمال Boost در PageDetail.tsx** [کامل شده ✅ — اما منطق آن در F2.7 بازآرایی می‌شود]
- **تسک F2.7 — دیباگ و یکپارچه‌سازی عینک لایو** [کامل شده ✅]

---

## ۴. رله کانتکست (Context Relay)

### وضعیت پروژه و کارهای قبلی:
- تسک‌های ساخت F2.1–F2.6 (تقویم شمسی، سرویس Pure، CSV با Zod، Context، UI تنظیمات، و Quick Toggle در PageDetail) پیاده شدند و اپلت کمپایل می‌شد.
- اما در جریان دیباگ‌های متوالی، منطق عینک به‌جای ماندن در سرویس Pure، داخل `PageDetail.tsx` جاسازی و در دو مسیر (نمایش/Gemini) واگرا شد. علائم گزارش‌شده توسط کاربر: «جدول فقط ۸ تور نشان می‌دهد»، «ماه‌های اشتباه نمایش داده می‌شوند»، «هر بار خراب‌تر شد».
- **تشخیص معمار:** الگوریتم پایه سالم است؛ تمام باگ‌ها در لایه عینک لایو هستند. شش قانون قطعی در `PROJECT.md`/`ARCHITECTURE.md §۱۰` منجمد شد و جدول باگ‌ها (A–H) + نقشه اصلاح در تسک F2.7 نوشته شد.
- **در تسک فعال (F2.7):** کل منطق عینک به `buildLiveOrderedList` (پایپ‌لاین تک‌مرجع) منتقل می‌شود؛ هر دو مسیر همان را صدا می‌زنند؛ پین=۴ بدون برش لیست؛ ضرایب صحیح (pre=×4، current=×3)؛ پنجره مجاز قطعی با استثنای رویداد جاری و فصل بعد ژنریک. `scorer.ts` و Dexie دست‌نخورده.
